<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.datn</groupId>
    <artifactId>quizai</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>quizai</name>
    <description>Ứng dụng Quiz/Trivia tích hợp trí tuệ nhân tạo</description>

    <properties>
        <java.version>21</java.version>
        <springdoc.version>2.8.17</springdoc.version>
        <jjwt.version>0.12.7</jjwt.version>
        <google-api-client.version>2.8.1</google-api-client.version>
        <tika.version>3.2.1</tika.version>
        <resilience4j.version>2.4.0</resilience4j.version>
        <testcontainers.version>1.21.3</testcontainers.version>
    </properties>

    <dependencies>
        <!-- Web / REST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- WebClient (gọi Gemini/Grok) + SSE streaming -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <!-- Real-time phòng đấu -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- Bảo mật -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- Dữ liệu -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-neo4j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Validation & vận hành -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Tài liệu API -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- JWT (HS256) -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Circuit breaker / retry cho lời gọi AI -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>

        <!-- Trích text từ học liệu PDF/DOCX/TXT cho pipeline RAG (features/05).
             Dùng tika-parsers-standard-package để có sẵn parser PDF và OOXML. -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
            <version>${tika.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
            <version>${tika.version}</version>
        </dependency>

        <!-- Gửi email OTP đặt lại mật khẩu (features/01 FR-4) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <!-- Xác minh ID token của Google khi đăng nhập bằng Google (FR-3).
             Dùng thư viện chính chủ chứ không tự viết: việc này gồm tải bộ khoá công khai của
             Google, chọn đúng khoá theo `kid`, xác minh chữ ký RS256 và kiểm iss/aud/exp — tự
             làm thì rất dễ sót một bước, mà sót bước nào cũng thành lỗ hổng cho phép giả token. -->
        <dependency>
            <groupId>com.google.api-client</groupId>
            <artifactId>google-api-client</artifactId>
            <version>${google-api-client.version}</version>
        </dependency>

        <!-- Tiện ích -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- @ServiceConnection: tự trỏ DataSource/Redis vào container trong test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>neo4j</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <!--
                          TEST KHÔNG ĐƯỢC ĐỤNG VÀO DỊCH VỤ THẬT TRÊN MÁY LẬP TRÌNH VIÊN.

                          PostgreSQL và Redis đã an toàn nhờ Testcontainers + @ServiceConnection ở
                          từng lớp test. Neo4j thì không: 11 lớp test khởi động cả ứng dụng nhưng
                          không khai báo Neo4jContainer, nên chúng lặng lẽ nối vào
                          bolt://localhost:7687 — đúng Neo4j dev đang chạy bằng docker compose.

                          Hậu quả nặng: lúc khởi động, GraphSchemaInitializer gọi
                          syncPublicCatalog(), và bước đó GỠ mọi nút không có trong PostgreSQL mà nó
                          đang nối tới. Với test thì đó là CSDL tạm gần như rỗng, nên mỗi lần chạy
                          `mvn test` là một lần XOÁ SẠCH đồ thị gợi ý của máy dev. Triệu chứng duy
                          nhất nhìn thấy được là "khu Gợi ý trống", rất dễ đổ oan cho code gợi ý.

                          Trỏ vào cổng không ai nghe là cắt hẳn đường đó. Ứng dụng vốn đã chịu được
                          Neo4j chết nên test vẫn chạy bình thường.

                          Đặt ở đây chứ KHÔNG phải src/test/resources/application.yml: file đó CHE
                          hẳn application.yml của main thay vì gộp vào, làm mất sạch cấu hình khác
                          (đã thử và hỏng ngay ở placeholder app.ai.gemini.model).

                          Lớp test nào thật sự cần Neo4j thì khai báo Neo4jContainer +
                          @ServiceConnection — bean ConnectionDetails được ưu tiên hơn thuộc tính.
                        -->
                        <spring.neo4j.uri>bolt://localhost:7699</spring.neo4j.uri>
                        <spring.neo4j.connection-timeout>1s</spring.neo4j.connection-timeout>
                        <spring.neo4j.pool.connection-acquisition-timeout>1s</spring.neo4j.pool.connection-acquisition-timeout>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
