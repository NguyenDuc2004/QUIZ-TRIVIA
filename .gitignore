# ==========================================================
#  General
# ==========================================================
.DS_Store
Thumbs.db
*.log
*.pid
*.seed
*.tmp
*.bak
*.swp
*~

# Environment variables / secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets.*
application-local.properties
application-local.yml
application-secret.*

# ==========================================================
#  IDE / Editor
# ==========================================================
.idea/
*.iml
*.ipr
*.iws
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.sample
.settings/
.project
.classpath
.factorypath
*.launch
nbproject/
.nb-gradle/

# ==========================================================
#  Java / Spring Boot (Maven & Gradle)
# ==========================================================
target/
build/
!**/src/main/**/target/
!**/src/test/**/target/
!**/src/main/**/build/
!**/src/test/**/build/

# Maven
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml

# Gradle
.gradle/
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties
.gradletasknamecache

# Compiled / packaged
*.class
*.jar
*.war
*.ear
!**/gradle/wrapper/gradle-wrapper.jar
!.mvn/wrapper/maven-wrapper.jar
hs_err_pid*
replay_pid*

# ==========================================================
#  Node / React (frontend)
# ==========================================================
node_modules/
dist/
dist-ssr/
build/
.next/
out/
coverage/
.nyc_output/
*.local

# Vite / bundler cache
.vite/
.cache/
.parcel-cache/
.turbo/
.eslintcache
.stylelintcache

# Package manager logs & lock debris
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ==========================================================
#  Docker / infra
# ==========================================================
*.env.docker
docker-compose.override.yml

# ==========================================================
#  Test / build reports
# ==========================================================
*.lcov
junit.xml
allure-results/
allure-report/
playwright-report/
test-results/

# ==========================================================
#  File người dùng tải lên (ảnh bìa quiz, ảnh câu hỏi)
# ==========================================================
backend/uploads/
uploads/

# Kết quả bộ quét lỗi web — sinh lại được bằng scripts/kiem-tra-web.mjs
bao-cao-datn/kiem-tra-web.json

# Tài liệu của ĐỒ ÁN KHÁC để tham khảo — KHÔNG đưa vào repo của đề tài này.
#
# Đã xảy ra hai lần: một lệnh `git add -A` quét luôn thư mục tham khảo mới thả vào, và báo cáo của
# sinh viên khác nằm trong lịch sử git của đồ án này — vừa phình repo, vừa là thứ không nên có ở đó
# khi báo cáo phải qua kiểm tra đạo văn. Lần đầu phải viết lại lịch sử mới gỡ được.
#
# Thêm dòng vào đây mỗi khi thả tài liệu tham khảo mới vào dự án.
tham khao/
docs/*EduExam*
docs/Poster.pptx
